package new

import AbstractAction
import AbstractGame
import IFilterManager

class FilterManager : IFilterManager {
    override fun filterActions(newAbstractGameState: AbstractGame, abstractActions: List<AbstractAction>): List<AbstractAction> {
        TODO("Not yet implemented")
    }
}